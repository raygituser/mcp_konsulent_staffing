apiVersion: 1
providers:
  - name: default
    folder: FastAPI
    type: file
    disableDeletion: true
    editable: true
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards
